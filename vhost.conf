<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /var/www/app/web

    <Directory /var/www/app/web>
        Options -Indexes
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

